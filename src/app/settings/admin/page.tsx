import Link from "next/link";
import { notFound } from "next/navigation";
import { getBusinessContext } from "@/features/team/data/business-context";
import { releaseNotes } from "@/features/admin/data/release-notes";
import { getRecentWorkspaceErrorReports } from "@/features/admin/data/error-reports";

export const dynamic = "force-dynamic";

export default async function AdminControlCenterPage() {
  const { role } = await getBusinessContext();
  if (role !== "owner" && role !== "admin") notFound();
  const errors = await getRecentWorkspaceErrorReports();

  return <div className="mx-auto max-w-5xl space-y-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-[var(--accent)]">Admin control center</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">Platform operations.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[var(--muted)]">A private place for workspace administrators to review releases and future operational controls. Financials remain owner-only.</p></div><Link href="/settings" className="min-h-11 rounded-2xl border border-white/10 px-4 py-3 text-sm font-semibold text-[var(--muted)] hover:bg-white/5 hover:text-white">← Back to settings</Link></div><section className="surface rounded-[2rem] p-5 sm:p-7"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-[var(--accent)]">Release status</p><h2 className="mt-2 text-xl font-semibold">v0.1.7 is installed</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted)]">Your workspace is on the latest verified release. Every update below is a production change, not just a planned feature.</p></div><span className="rounded-full border border-emerald-400/25 bg-emerald-400/10 px-3 py-1.5 text-xs font-bold text-emerald-200">● Current</span></div></section><section className="surface rounded-[1.75rem] p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-[var(--accent)]">Workspace health</p><h2 className="mt-2 text-xl font-semibold">Recent safe diagnostics</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted)]">Only signed-in workspace errors are recorded. Reports exclude stacks, passwords, item details, and financial data.</p></div><span className={`rounded-full px-3 py-1.5 text-xs font-bold ${errors.length ? "border border-amber-400/25 bg-amber-400/10 text-amber-100" : "border border-emerald-400/25 bg-emerald-400/10 text-emerald-200"}`}>{errors.length ? `${errors.length} recent` : "● Clear"}</span></div>{errors.length ? <div className="mt-5 divide-y divide-white/8 rounded-2xl border border-white/8">{errors.map((report) => <article key={report.id} className="p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm font-semibold">{report.source}</p><p className="mt-1 text-sm text-[var(--muted)]">{report.message}</p></div><time className="text-xs text-[var(--muted)]" dateTime={report.occurredAt}>{new Intl.DateTimeFormat("en-US", { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" }).format(new Date(report.occurredAt))}</time></div></article>)}</div> : <p className="mt-5 rounded-2xl border border-dashed border-white/10 px-4 py-5 text-sm text-[var(--muted)]">No app errors have been reported by this workspace.</p>}</section><section className="space-y-3"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-[var(--accent)]">Update log</p><h2 className="mt-2 text-xl font-semibold">What changed, in detail.</h2></div>{releaseNotes.map((release, index) => <article key={release.version} className="surface rounded-[1.75rem] p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-3"><h3 className="font-semibold">{release.version} · {release.summary}</h3>{index === 0 && <span className="rounded-full bg-[var(--accent)] px-2.5 py-1 text-[10px] font-black uppercase tracking-[.12em] text-black">Current</span>}</div><p className="mt-1 text-sm text-[var(--muted)]">Installed {release.releasedOn}</p></div><span className="rounded-full border border-white/10 px-2.5 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-[var(--muted)]">{release.status}</span></div><ul className="mt-5 space-y-2 text-sm leading-6 text-[var(--muted)]">{release.changes.map((change) => <li key={change} className="flex gap-3"><span className="mt-2 size-1.5 shrink-0 rounded-full bg-[var(--accent)]"/>{change}</li>)}</ul></article>)}</section><section className="rounded-[1.75rem] border border-white/10 bg-white/[.03] p-5 sm:p-6"><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--accent)]">Admin access</p><p className="mt-2 text-sm leading-6 text-[var(--muted)]">The owner can designate or remove admins from Settings → Team & access. Admins can use this control center, but cannot see owner finances or change who owns the business.</p></section></div>;
}
